/*const nodemailer = require('../server/src/nodemailer'); 

 test('transporter verified', () => {
    const consoleSpy = jest.spyOn(console, 'log');
    console.log('Nodemailer working.');
    expect(consoleSpy.toHaveBeenCalledWith('Nodemailer working.'));
 });
 */